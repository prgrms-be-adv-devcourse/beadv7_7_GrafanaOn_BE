server:
  port: 8080

spring:
  application:
    name: gateway-service

  profiles:
    active: local

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway

  endpoint:
    gateway:
      access: read-only